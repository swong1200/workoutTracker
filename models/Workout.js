const mongoose = require("mongoose");
const Schema = mongoose.Schema;

const WorkoutSchema = new Schema({
    day: {

    },
    exercises: [
        {
        type: 
        name:
        duration:
        distance:
        weight:
        reps:
        sets:
    }
]
});